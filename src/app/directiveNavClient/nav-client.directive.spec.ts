import { NavClientDirective } from './nav-client.directive';

describe('NavClientDirective', () => {
  it('should create an instance', () => {
    const directive = new NavClientDirective();
    expect(directive).toBeTruthy();
  });
});
